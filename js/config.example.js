// Template — copy to config.js and fill in your key.
// config.js is gitignored; never commit real keys.
window.AI_DEPLOY_CONFIG = {
  endpoint: 'https://api.deepseek.com/chat/completions',
  apiKey: 'YOUR_KEY_HERE'
};
